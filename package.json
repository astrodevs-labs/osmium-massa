{
  "name": "osmium-massa",
  "private": true,
  "workspaces": [
    "libs/*",
    "vscode",
    "sidebar"
  ],
  "devDependencies": {
    "turbo": "^1.12.4"
  },
  "scripts": {
    "build": "turbo build --concurrency 3 && node ./scripts/copy-front.js",
    "build:extension": "turbo build:extension --concurrency 3 && node ./scripts/copy-front.js",
    "publish": "turbo publish",
    "publish:check": "turbo publish:check",
    "publish:extension": "turbo publish:extension",
    "test": "turbo test",
    "format": "turbo format",
    "format:check": "turbo format:check",
    "lint": "turbo lint",
    "lint:check": "turbo lint:check"
  }
}
